{
  "name": "linear-actions",
  "version": "0.1.0",
  "description": "GitHub Action helpers for Linear issues",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && ncc build lib/index.js -o dist",
    "clean": "rm -rf lib dist",
    "prepare": "npm run build",
    "test": "vitest"
  },
  "keywords": [
    "github-action",
    "linear",
    "project-management"
  ],
  "files": [
    "action.yml",
    "dist",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=18"
  },
  "author": "",
  "dependencies": {
    "@actions/core": "^1.11.1"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@vercel/ncc": "^0.38.4",
    "typescript": "^5.9.3",
    "vitest": "^4.0.8"
  }
}
